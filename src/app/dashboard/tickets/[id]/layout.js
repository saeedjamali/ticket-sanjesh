// export async function generateMetadata({ params }) {
//   return {
//     title: `تیکت ${params.id}`,
//     description: "جزئیات تیکت پشتیبانی",
//   };
// }

export default function TicketLayout({ children }) {
  return children;
}
